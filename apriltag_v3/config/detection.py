"""검출 — 태그를 보고 위치를 내는 데 쓰는 값.

여기 없는 것: fx/fy/cx/cy 는 카메라가 직접 준다. 태그·카메라 높이는
실사용에 안 쓴다(자세에 이미 들어 있다) — config/sim.py.

계산으로 나오는 값은 상수로 박지 않는다. 배치가 바뀔 때 안 따라온다.
값의 근거는 ../../code_explanation.md 참고.

    ★ 현장에서 확정할 것    △ 실측 후 조정 가능
"""
# 재수출이다 — 이 모듈에서 직접 안 써도 지우지 마라. `config.main` 의 `import *` 와
# `detection_pose.py` 의 30프레임 상수(STABLE_* = D.LAT_TOL_M/3 …)가 여기를 거쳐 읽는다.
from .control import HEAD_TOL_DEG, LAT_TOL_M   # noqa: F401

# ── 무엇을 보나 ─────────────────────────────────────────────────────────────
TAG_SIZE_M = 0.300             # 태그 한 변 [m], 검은 테두리 바깥까지  ★인쇄물 실측
TAG_ID = 1                     # 멀리서 접근할 때 보는 태그
COLOR_SIZE = (1920, 1080)      # D435i 컬러 최대. 프레임당 12.9ms
IR_SIZE = (1280, 720)          # IR 은 fx 가 컬러의 1/3 이라 거리도 1/3

# ── 이 프레임을 믿어도 되나 ─────────────────────────────────────────────────
MIN_TAG_PX = 20.0              # 태그 한 변이 이보다 작으면 자세를 안 믿는다
STABLE_TAG_PX = 30.0           # 이 이상이면 안정적                    △실장비 검증 전
MAX_REPROJ_RMS_PX = 2.0        # 재투영 오차 상한 [px]
MIN_DECISION_MARGIN = 20.0     # 저조도 하한 (거리에는 둔감하다)
RELIABLE_TILT_DEG = 10.0       # 옛 각도 신뢰 판정. 지금은 폴백 전용
DEFAULT_QUAD_BLUR = 0.0        # 검출 전 블러. 안 넣는 게 최선이었다


# ── 각도를 몇 도 오차로 아는가 ──────────────────────────────────────────────
# tilt 로 판단하면 안 된다 — 정면 접근에선 거리와 무관하게 늘 0 이라 각도를
# 영영 못 쓴다. 코너 잡음을 실제로 흘려보고 heading 흔들림을 예측한다.
CORNER_NOISE_PX = 0.07         # 코너 검출 잡음 [px]                   △거리마다 다름
SIGMA_SAMPLES = 60             # 그 예측에 쓰는 몬테카를로 표본 수

# ── 대표값 하나를 만들 때 ───────────────────────────────────────────────────
# MEASURE_FRAMES·MEASURE_MAX_FRAMES·STABLE_LATERAL_M·STABLE_HEADING_DEG·
# MAX_HEADING_SIGMA_DEG·STABLE_SPREAD_K 는 2026-09-21 에 여기서 **뺐다**
# (plan 4-3 상수 회계: "→ VERIFY 전용 코드 내부"). 지금은
# `src/models/detection/detection_pose.py` 맨 위 모듈 상수다 — 30프레임 중앙값은
# v3 에서 정지 후 확인(VERIFY)에만 쓰고, 주행 중에는 연속 추정기가 답을 낸다.
# 허용치에서 파생되는 관계(LAT_TOL_M/3 등)는 거기서 그대로 유지한다.

# ── 카메라 장착 보정 ────────────────────────────────────────────────────────
# 미리 못 잰다 — IMU 가 카메라에 붙어 같이 틀어져 있고 중력은 yaw 를 모른다.
# 도킹이 매번 같은 방향으로 삐뚤면 그 각도를 넣을 것. 랜덤이면 이 문제가 아니다.
CAM_YAW_OFFSET_DEG = 0.0       # 광축이 지게차 정면과 어긋난 각 [도]   ★첫 도킹 후

# ── 장비 규격 (근거 확정. 바꾸면 틀린다) ────────────────────────────────────
BLUR_CLEAN_PX = 10.0           # 여기까지 검출 100%
BLUR_DEAD_PX = 32.0            # 여기서 0%
COLOR_EXPOSURE_UNIT_US = 100.0 # 컬러 노출 눈금 (UVC 규격). 83 = 8.3ms
TAG_CELLS = 8                  # tag36h11 한 변의 칸 수 (테두리 포함)
MM_PER_INCH = 25.4
D435I_COLOR_REF = (1920, 1080, 1359.2, 1359.0, 956.9, 571.3)   # w,h,fx,fy,cx,cy

