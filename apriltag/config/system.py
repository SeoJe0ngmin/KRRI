"""직접 정한 숫자를 한자리에 모은다.

세 부류가 섞여 있다. 성격이 다르니 다루는 법도 다르다.
    1) 현장값     현장이 바뀌면 바꾼다
    2) 판정 임계   실장비로 확인한 뒤 조정한다
    3) 측정·규격   근거가 있다. 바꾸면 틀린다

여기 없는 것
    fx/fy/cx/cy       카메라가 직접 준다. 적어두면 해상도 바꿀 때 어긋난다
    FWD_*             다른 팀 모델. src/models/control/fwd_time_model.py
    태그·카메라 높이   실사용에는 안 쓴다(자세에서 직접 나온다). config/sim.py 로 뺐다
    멈출 거리          숫자로 안 정한다. 태그가 화면에서 잘리기 직전까지 간다

**계산으로 나오는 것은 여기 두지 않는다.** 손으로 박으면 배치가 바뀔 때 안 따라온다.
실제로 카메라 높이를 1.20 -> 0.50m 로 바꾸니 예전 STOP_M=1.50 이 근접 한계(2.55m)보다
작아져서 매번 태그를 잃는 값이 됐다. 그래서 지금은 화면을 직접 본다.
"""
from math import atan as _atan, degrees as _deg

# ── 1) 현장값 ───────────────────────────────────────────────────────────────

TAG_SIZE_M = 0.200             # 태그 한 변 [m]. 검은 테두리 바깥까지. 인쇄물 실측 확인(2026-08-30)
TAG_ID = 1                     # 도킹에 쓸 태그 번호

COLOR_SIZE = (1920, 1080)      # D435i 컬러 최대. 20cm 태그 안정검출 5.4m, 프레임당 12.9ms
IR_SIZE = (1280, 720)          # IR 은 fx 가 컬러의 1/3 이라 거리도 1/3


# ── 2) 판정 임계 ────────────────────────────────────────────────────────────

MIN_TAG_PX = 20.0              # 태그 한 변이 이보다 작으면 자세를 안 믿는다. 19.5px 에서 검출률 89.3%
STABLE_TAG_PX = 50.0           # 이 이상이면 안정적. 통념값, 실장비 검증 전
MAX_REPROJ_RMS_PX = 2.0        # 재투영 오차 상한. 코너 노이즈 바닥 0.35px 의 약 6배
MIN_DECISION_MARGIN = 20.0     # 저조도 하한으로만 쓴다. 거리에는 둔감하다(238->19.5px 에도 71.8~73.2)
RELIABLE_TILT_DEG = 10.0       # 각도를 믿을 최소 기울기. tilt 2도면 좌우 변 차이가 0.3px
CORNER_NOISE_PX = 0.07         # 코너 검출 잡음 [px]. before.json 30프레임에서 역산
                               # (lateral 3.83mm / heading 0.156도 가 둘 다 0.07 을 가리킴)
# 아래 셋은 허용치에서 나온다. "측정 흔들림은 맞춰야 할 값보다 한참 작아야 한다."
# 따로 박아두면 허용치를 바꿀 때 안 따라와서 서로 안 맞는 조합이 된다.
SIGMA_SAMPLES = 60             # 그 예측에 쓰는 몬테카를로 표본 수
DEFAULT_QUAD_BLUR = 0.0        # 검출 전 가우시안 블러. 9가지 비교에서 안 넣는 게 최선이었다

DEPTH_TOL_COEF = 0.05          # depth 대조 허용치 = max(FLOOR, COEF*z^2). depth 오차가 z^2 로 커진다
DEPTH_TOL_FLOOR_M = 0.02       # 그 하한 [m]
DEPTH_CHECK_MAX_Z = 1.5        # 이 거리 넘으면 허용치가 헐거워 판정이 무의미 [m]

MEASURE_FRAMES = 30            # 대표값 하나를 낼 때 모으는 프레임 수. 30이면 흔들림이 1/5.5
MEASURE_MAX_FRAMES = MEASURE_FRAMES * 5    # 이만큼 봐도 못 모으면 포기(태그를 놓친 것)


# ── 3) 측정·규격 ────────────────────────────────────────────────────────────

BLUR_CLEAN_PX = 10.0           # 실측: 여기까지 검출 100%
BLUR_DEAD_PX = 32.0            # 실측: 여기서 0%. 12px 96% / 20px 89% / 28px 50%
COLOR_EXPOSURE_UNIT_US = 100.0 # 컬러 노출 눈금. UVC 규격. 83=8.3ms. 뎁스/IR 은 us 라 100배 다르다
LUMA_CLIPPED_LEVELS = 38       # bgr8 의 BT.601 변환이 죽이는 휘도 단계 (256 중 15%)
TAG_CELLS = 8                  # tag36h11 한 변의 칸 수(검은 테두리 포함)
MM_PER_INCH = 25.4

D435I_COLOR_REF = (1920, 1080, 1359.2, 1359.0, 956.9, 571.3)   # 다른 해상도는 세로 비율로 스케일(실측 0.03px 이내)

# 화각은 위 fx 에서 그대로 나온다 — 따로 적어두면 둘이 어긋난다.
# 카메라가 있으면 이 값도 안 쓴다(SDK 가 주는 fx 를 쓴다). 영상 파일용 대체값이다.
ASSUMED_HFOV_DEG = 2.0 * _deg(_atan(D435I_COLOR_REF[0] / (2.0 * D435I_COLOR_REF[2])))

# ── 4) 도킹 (control_from_pose.py) ──────────────────────────────────────────

LAT_TOL_M = 0.030              # |lateral| 이 이보다 작으면 됐다고 본다. 명령 하한이 17mm 라 그 밑은 무의미
HEAD_TOL_DEG = 2.0             # heading 허용치. 옆이동 3단계가 자동으로 0 으로 만든다

STABLE_LATERAL_M = LAT_TOL_M / 3.0        # 대표값 표준오차가 이보다 크면 명령을 안 낸다
STABLE_HEADING_DEG = HEAD_TOL_DEG / 3.0   # 위와 같음 [도]
MAX_HEADING_SIGMA_DEG = HEAD_TOL_DEG / 4.0  # 예측 흔들림이 이보다 크면 각도를 안 믿는다
STABLE_SPREAD_K = 3.0          # 관측 흔들림이 예측의 이 배를 넘으면 뭔가 모르는 일이 있는 것
                               # (진동·부분 가림·모션블러). 기하 예측으로는 못 잡는다
STEP_M = 1.0                   # 직진 한 조각. 3m 를 1m 씩이면 1.46배 느려짐(측정 포함)
# 멈출 거리를 숫자로 안 정한다. 태그가 화면에서 잘리기 직전까지 가고, 그때
# 다음 태그로 넘긴다. 마지막 태그면 남은 forward + DOCK_EXTRA_M 만큼 가고 선다.
TAG_CUT_MARGIN_PX = 30.0       # 태그가 화면 가장자리에서 이만큼 안으로 오면 "잘리기 직전"
DOCK_EXTRA_M = 0.0             # 마지막 태그 면을 지나 더 갈 거리 [m]. 태그가 탑재부 끝이 아니면 양수
FWD_SAFETY = 0.9               # 한 번에 명령하는 거리 = 남은 거리 x 이 값. 지나치느니 한 번 더 잰다
WARMUP_FRACTION = 0.33         # 맨 처음엔 lateral 말고 이 비율만큼 먼저 다가간다 (가까울수록 정확해서)
FWD_ABORT_K = 3.0              # 전진 중 heading 감시: 허용치 = max(기하허용, 이 값 x 프레임잡음)
SIDESTEP_BACKWARD_GAIN_DEG = 0.0   # 후진이 회전을 이만큼 넘게 아끼면 후진. 0 = 회전 작은 쪽
MAX_STEPS = 30                 # 이 단계 수를 넘기면 수렴 실패로 보고 멈춘다
HOLD_RETRY_SEC = 0.2           # hold 판정 뒤 다시 재기까지 (dock 전용. dock_live 는 프레임이 곧 주기)
SETTLE_SEC = 0.15              # 시간 명령을 끊은 뒤 관성이 잦아들 시간

# ── 5) 회전 (IMU 폐루프) ────────────────────────────────────────────────────
#
# 회전은 시간이 아니라 각도로 명령한다 — CanDriver.rotate_by() 가 IMU 를 보며
# 목표각에서 끊는다. 아래 ROT_T0_SEC / ROT_DEG_PER_SEC 는 **실행에 안 쓴다**.
# 화면의 "예상 N초", 워치독 상한, IMU 가 없을 때의 비상 폴백에만 남아 있다.

ROT_T0_SEC = 0.5               # 회전 명령 지연 [s].  **미측정 가정값** (직진 t0=0.507 을 빌림)
ROT_DEG_PER_SEC = 15.0         # 회전 각속도 [도/s].  **미측정 가정값**
ROT_MIN_SEC = 1.0              # 직진과 같다고 가정. 확인 필요
ROT_MAX_SEC = 15.0             # 워치독 하한. 실제 상한은 각도에 비례해 늘어난다(rot_timeout_sec)
ROT_WATCHDOG_GAIN = 3.0        # 워치독 = 예상시간 x 이 값. 각속도가 1/3 이어도 버틴다
ROT_LEAD_DEG = 0.0             # 관성만큼 미리 끊는 각. 0 이면 안 끊음.
                               # rotate_by 가 회전마다 overshoot 를 재서 로그로 남긴다 — 그 평균을 넣을 것
ROT_WRONG_WAY_DEG = 5.0        # 반대로 이만큼 돌면 CAN 회전 부호가 뒤집힌 것. 즉시 정지
ROT_POLL_SEC = 0.01            # 회전 중 IMU 확인 주기. 10ms 면 15도/s 에서 0.15도/틱
ROT_SETTLE_MAX_SEC = 1.0       # 정지 명령 후 멎기를 기다리는 상한
ROT_SETTLE_MIN_SEC = 0.2       # 최소 이만큼은 기다린다 (명령 반영 지연)
ROT_SETTLE_POLL_SEC = 0.05     # 멎었나 확인하는 주기
ROT_SETTLE_RATE_K = 3.0        # 멎음 판정 = 보정 때 잰 잡음 x 이 값 [도/s]
ROT_SETTLE_RATE_FLOOR = 1.0    # 그 판정의 하한 [도/s]. 보정 전이면 이 값을 쓴다

# ── 6) IMU (자이로) ─────────────────────────────────────────────────────────

IMU_GYRO_HZ = 200              # 자이로 샘플 주파수. 이 장치의 유효값은 200/400 뿐
IMU_YAW_SIGN = -1.0            # 자이로 y축 -> 우리 부호규약(+가 반시계). 팀원 실측 확정
IMU_BIAS_SEC = 2.0             # 정지 바이어스 측정 시간 [s]
IMU_STALE_SEC = 0.3            # 이만큼 샘플이 안 오면 죽은 것으로 본다
IMU_DT_GAP_SAMPLES = 10        # 샘플 간격이 이 개수를 넘게 벌어지면 그 구간은 적분하지 않는다
IMU_CALIB_MIN_RATIO = 0.5      # 보정 샘플이 기대치의 이 비율은 와야 통과
IMU_MOVING_DPS = 0.5           # 보정 중 자이로 표준편차/평균이 이보다 크면 움직인 것

# ── 7) 태그를 잃었을 때 (최종 안전망) ───────────────────────────────────────

SEARCH_AFTER_MISSES = 3        # **아무 태그도** 못 본 사이클이 이만큼이면 탐색 시작
SEARCH_BACKUP_M = 0.5          # 쓸어도 못 찾으면 이만큼 후진 (세로 시야가 넓어지고 방위각도 준다)
SEARCH_MAX_ROUNDS = 3          # 쓸기+후진을 이만큼 하고 못 찾으면 정지하고 사람을 부른다

# ── 8) 태그 전환 ────────────────────────────────────────────────────────────

TAG2_ID = 2                    # 근접 구간에서 갈아탈 태그. 없으면 None

# ── 9) 카메라 장착 보정 ─────────────────────────────────────────────────────

CAM_YAW_OFFSET_DEG = 0.0       # 카메라 광축이 지게차 정면과 어긋난 각 [도]. heading 에서 뺀다
                               # 실측 전까지 0. 재는 법은 docking_state 독스트링 참고
                               # roll/pitch 어긋남은 도킹값에 영향이 없어 보정 항목이 없다
