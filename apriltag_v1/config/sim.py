"""시뮬레이터 전용 값. **실사용에는 안 쓴다.**

실제로 도킹할 때는 태그·카메라 높이를 알 필요가 없다 — 자세(T_camera_tag)에
이미 다 들어 있고, 멈출 거리도 화면에서 태그가 잘리기 직전을 보고 정한다.
여기 값은 "가상의 장면을 만들 때" 만 쓴다: 태그를 어디에 두고 카메라를 어디에
둘지 지어내야 정답을 만들 수 있기 때문이다.

    tools/sim.py          이 값으로 장면을 만들고 검출 오차를 잰다
    tools/sim_engine.py   장면 -> 4x4 자세 변환
    tools/sim_measure.py  장면 -> 그림 -> 검출 -> 오차

현장 배치를 바꿔 보고 싶으면 여기만 고치면 된다. 높이차가 클수록 태그가
화면 위로 밀려 나가므로 "더 멀리서부터만 보인다".
"""

# 2026-09-06 실제 설치 예정 배치
#
# **중심 높이다, 아랫변이 아니다.** 현장에서 재는 건 보통 아랫변이므로
# 태그 크기의 절반을 더해서 적을 것:
#     태그 아랫변 1.35m + 0.30m/2 = 중심 1.50m
TAG_HEIGHT_M = 1.50            # 바닥 기준 태그 **중심** 높이 [m] (아랫변 1.35m)
CAM_HEIGHT_M = 0.63            # 바닥 기준 카메라 높이 [m]
                               # 높이차 0.87m / 30cm 태그 ->
                               #   근접 한계 2.43m (이보다 가까우면 화면 위로 잘림)
                               #   안정 검출 8.16m (태그폭 50px)
                               # 카메라를 위로 기울이면 근접 한계가 당겨진다:
                               #   5도 1.93m / 10도 1.58m / 15도 1.31m
                               #   (아래 시야는 20.5도에서 그만큼 줄어든다)
