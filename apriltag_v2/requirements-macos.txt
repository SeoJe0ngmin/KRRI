# 맥북 전용 — Apple Silicon (M1~), macOS 15 Sequoia 이상, Python 3.11
#
#     python3.11 -m venv .venv && source .venv/bin/activate
#     pip install -r requirements-macos.txt
#     python tools/check_setup.py
#
# 맥북으로 되는 것 / 안 되는 것 (2026-09-06 macOS 26.3 실측)
#     된다   검출, 시뮬레이션, dry-run 주행, 기록 분석,
#            카메라는 **컬러만** — tools/live_pose.py --source webcam
#            (macOS 가 RealSense 컬러를 일반 UVC 웹캠으로 띄워 주는 걸 OpenCV 로 연다)
#     안 된다 RealSense SDK 로 실카메라 열기 = depth, IR, IMU, 노출 메타데이터, bag 녹화.
#            macOS 12+ 는 시스템 UVCAssistant 가 카메라를 선점해 librealsense 가
#            'failed to set power state' 로 못 잡고(sudo 필요), sudo 로 뺏어도
#            2.56.5 는 IMU 초기화에서 segfault 난다(librealsense #14302, 미해결).
#            실주행(CAN) — Kvaser 가 macOS 에 CANlib API 를 안 낸다.
#            MacCAN 드라이버가 있지만 CAN API V3 라는 다른 규격이라
#            control_forklift_v2.py 가 그대로는 못 쓴다. 실주행은 Windows 로.
#
# requirements.txt 와 값은 같다 — 그쪽은 마커로 OS 를 가르고, 이 파일은
# 맥북만 놓고 풀어 쓴 것이다. 버전을 고칠 때는 두 파일을 같이 고칠 것.

numpy==2.4.6
opencv-python==5.0.0.93
pupil-apriltags==1.0.4.post11
pyrealsense2-macosx==2.56.5        # 공식 pyrealsense2 는 macOS 휠이 없다.
                                   # import 이름은 그대로 pyrealsense2 라 코드는 안 고친다.
                                   # 실카메라는 못 연다(위 참고) — import 와 bag 재생용으로만 둔다

# 아래 넷은 분석용처럼 보이지만 **지우면 검출이 죽는다**
# (detection_pose.py -> utils/util.py 가 최상단에서 import 한다)
matplotlib==3.11.1
scipy==1.17.1
shapely==2.1.2
ipython==9.16.1

# canlib / keyboard 는 일부러 뺐다 — 위 "안 되는 것" 참고.
# keyboard 가 없으면 run.py 는 SPACE 대신 엔터로 시작한다(자동 폴백).
